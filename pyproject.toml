[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "contidesigner"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas", 
    "plotly",     # plotting belongs to model package
    "scipy",       # solvers
    #"contextlib",  # for context management in solvers
]
